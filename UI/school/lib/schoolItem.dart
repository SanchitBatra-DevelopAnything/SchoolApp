import 'package:flutter/material.dart';

class SchoolItem extends StatelessWidget {
  const SchoolItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
